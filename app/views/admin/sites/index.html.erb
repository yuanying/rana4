<h1>Listing admin_sites</h1>

<table>
  <tr>
    <th><%=t 'activerecord.attributes.site.title'%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_sites.each do |admin_site| %>
  <tr>
    <td><%= admin_site.title %></td>
    <td><%= link_to 'Show', [:admin, admin_site] %></td>
    <td><%= link_to 'Edit', edit_admin_site_path(admin_site) %></td>
    <td><%= link_to 'Destroy', admin_site, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site', new_admin_site_path %>
